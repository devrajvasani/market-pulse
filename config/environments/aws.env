# Real AWS — NO secrets in this file. AWS credentials come from ~/.aws
# (named profile or SSO), never from here (plan Section O.2).
APP_ENV=aws
ENVIRONMENT=dev
LOG_LEVEL=INFO

AWS_DEFAULT_REGION=us-east-1
# AWS_ENDPOINT_URL is intentionally unset on AWS so boto3 uses the real endpoints.
